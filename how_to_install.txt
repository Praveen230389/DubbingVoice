# install your git repo first dubbingvoices

!git clone https://github.com/Praveen230389/DubbingVoice.git

# install these

!pip install omnivoice
!pip install srt
!pip install pydub
!pip install soundfile
!pip install gradio

# load the model

from omnivoice import OmniVoice
import torch

model = OmniVoice.from_pretrained(
    "k2-fsa/OmniVoice",
    device_map="cuda:0",
    dtype=torch.float16
)

# then finally run script

exec(open("DubbingVoice/srt_dubber.py").read())

# if running gradio

exec(open("app.py").read())

or 

!python app.py

  
