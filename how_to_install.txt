# install your git repo first dubbingvoices

!pip -q install git+https://github.com/Praveen230389/DubbingVoice.git

# install these

!pip install omnivoice
!pip install srt
!pip install pydub
!pip install soundfile
!pip install gradio

# load the model

from omnivoice import OmniVoice
import torch

model = OmniVoice.from_pretrained(
    "Praveen230389/OmniVoice",
    device_map="cuda:0",
    dtype=torch.float16
)

# then finally run script

  exec(open("srt_dubber.py").read())

  
